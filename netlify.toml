[build]
  command = "npm run build"
  publish = "dist/public"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["hono"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
  force = true
